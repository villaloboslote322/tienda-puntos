version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: tienda-puntos-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tienda_puntos
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: tienda-puntos-backend
    environment:
      NODE_ENV: development
      PORT: 3001
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/tienda_puntos
      JWT_PRIVATE_KEY: ${JWT_PRIVATE_KEY:-dev-private-key}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:-dev-public-key}
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: npm run dev
    networks:
      - tienda-puntos-network

networks:
  tienda-puntos-network:
    driver: bridge

volumes:
  postgres_data:
